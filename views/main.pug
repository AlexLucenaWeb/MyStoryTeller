extends base
include _bookBoxes

block content
    main.main
        .main__title
            h2 Top books
        .bookBox-container
                each book in books
                    +bookBoxes(book)