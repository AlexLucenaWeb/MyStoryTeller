extends base
include _favoriteBoxes


block content
    main.main
        .main__title
            img.title__img(src="/img/title-bg.jpg")
            h2.title My list
        .main__section
            .bookBox-container
                each favorite in user.favorites
                    +favoriteBoxes(favorite)
