extends base
include _reviewBoxes

block content
    main.main
        .section.bookOverview-container
            .bookOverview__name.container
                .row
                    h1.col-12  
                        span= book.name
            .bookOverview__info.container  
                .row
                    .bookOverview__image.col-6
                        img.bookOverview__cover(src=`/img/books/${book.imageCover}` alt=`${book.imageCover}`)
                    .bookOverview__data.col-6
                        p.bookOverview__description
                            span= book.description
                        p.bookOverview__rating
                            span= book.ratingsAverage
                        p.bookOverview__age
                            span= book.age
                    button#createReview Create review
                    a(href=`/book/${book.slug}/read`) Read the book
        .section.review-container.container
            .review
                .reviewBoxOne
                each review in book.reviews
                    +reviewBoxes(review)
        //- script(type="text/javascript").
        //-     import axios from 'axios';
        //-     const favoriteBtn = document.querySelector('#addFavorites');
        //-     favoriteBtn.addEventListener('click', async() =>{
        //-             try{
        //-                 const res = await axios({
        //-                     method: 'PATCH',
        //-                     url: `http://127.0.0.1:3000/api/v1/books/${book.id}/addFavorites`
        //-                 });
        //-                 //- if (res.data.status === 'success') {
        //-                 //-     console.log("siiiii funciona")
        //-                 //-     //- showAlert('success', `The book is added to your list`);
        //-                 //- }
        //-             } catch (err) {
        //-                 console.log(err);
        //-             }
        //-         });